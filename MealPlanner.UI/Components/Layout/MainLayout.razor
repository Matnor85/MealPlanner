@inherits LayoutComponentBase

<div class="app-shell">
    <header class="app-header">
        <span class="app-title">Matplaneraren</span>
    </header>

    <main class="app-body">
        @Body
    </main>

    <nav class="tab-bar five">
        <NavLink class="tab" href="" Match="NavLinkMatch.All">
            <span class="tab-icon">📅</span>
            <span class="tab-label">Vecka</span>
        </NavLink>
        <NavLink class="tab" href="recipes">
            <span class="tab-icon">📖</span>
            <span class="tab-label">Recept</span>
        </NavLink>
        <NavLink class="tab" href="pantry">
            <span class="tab-icon">🥫</span>
            <span class="tab-label">Skafferi</span>
        </NavLink>
        <NavLink class="tab" href="shopping">
            <span class="tab-icon">🛒</span>
            <span class="tab-label">Inköp</span>
        </NavLink>
        <NavLink class="tab" href="profile">
            <span class="tab-icon">👤</span>
            <span class="tab-label">Profil</span>
        </NavLink>
    </nav>
</div>